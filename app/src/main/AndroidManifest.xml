<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.votafore.warlords">

    <!-- для работы  с wifi -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".v4.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="landscape"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- v4
        <activity android:name=".ActivityMain">
        </activity>
        <activity android:name=".ActivityTest">
        </activity>
        -->

        <activity android:name=".v4.activities.ActivityStart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".v4.activities.ActivityGameLocal"/>
        <activity android:name=".v4.activities.ActivityGameRemote"/>
        <activity android:name=".v4.activities.ActivityGame"/>
        <activity android:name=".v4.activities.ActivityGameOver"/>


    </application>

</manifest>